#include<stdio.h>
int main()
{
    printf(
     "git push test"
    );//This file for testing push the folder under the main repository. To do this we need to put this instruction :git add . git commit -m "message" git push origin main(if do from main repository do git push only)

    return 0;
}